# TanStack Start + pnpm: the default isolated/global virtual store can 404
# `virtual:tanstack-start-client-entry`. A hoisted node_modules avoids it.
node-linker=hoisted
# Let Vite/TanStack resolve peers without hard failures during scaffolding.
strict-peer-dependencies=false
